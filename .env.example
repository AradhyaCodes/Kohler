# Copy to .env and fill ONE of these. With no key the app still works (rule-based mode).
# GEMINI_API_KEY=xxxxxxxx
# Optional: override the model litellm uses
# LLM_MODEL=openrouter/openai/gpt-4o-mini
OPENROUTER_API_KEY=
python -m streamlit run app.py